

const initialState = {
  login: '',
  password: '',
  sublogin: '',
};

export default function formAuthReducer(state = initialState, action) {
  switch (action.type) {
    case :
      return {
        ...state,
        
      };
    default:
      return state;
  }
}